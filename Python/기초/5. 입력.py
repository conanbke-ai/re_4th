# 입력 : input() 함수
'''
사용자로부터 콘솔을 통해 문자열 형태의 입력을 받는 함수
입력은 항상 문자열(str) 형태로 반환임 → 숫자 계산을 위해서는 형 변환(int() 또는 float()) 필요
'''
"""
int(input()) 동작 과정:
1. input()으로 사용자 입력 받음 → 문자열 "85"
2. int()로 문자열을 정수로 변환 → 숫자 85
3. 변수에 저장하고 계산에 사용 가능
"""

name = input("이름을 입력하세요 \n" + ": ")
print(name)

score = input("점수를 입력하세요 \n" + ": ")
print(f'점수는  : {score}')


# 1. 문자열 입력받기
print("다음 코드를 실행하면 입력을 기다립니다:")
print('name = input("이름을 입력하세요 : ")')
print('print(name)')

# 실제 입력 예시 (주석으로 설명)
# name = input("이름을 입력하세요 : ")  # 사용자가 "김철수" 입력
# print(name)                        # 출력: 김철수

# 2. 숫자 입력받기 (문자열을 정수로 변환)
print('\n다음 코드를 실행하면:')
print('score = int(input("점수를 입력하세요 : "))')
print("f'점수는 : {score + 10}'")

# 실제 입력 예시 (주석으로 설명)
# score = int(input("점수를 입력하세요 : "))  # 사용자가 "85" 입력
# print(f'점수는 : {score + 10}')            # 출력: 점수는 : 95


# split() : 문자열을 구분자로 나누는 함수 (list로 나뉘어짐)
'''
문자열만 사용 가능한 함수(숫자 사용 불가)
split 함수 괄호 안에 구분자 지정 가능 / 기본값 : 공백
'''
fruit = "사과 참외 수박".split()
print(fruit)

fruit1, fruit2, fruit3 = input().split()
print(fruit1, fruit2, fruit3, sep=", ")

print("\n=== 문자열 분리: split() 함수 ===")

"""
split() 함수:
- 문자열을 특정 구분자를 기준으로 나누는 함수
- 기본값은 공백(스페이스)을 구분자로 사용
- 결과는 리스트(여러 값들의 모음)로 반환
"""

# 기본 split() 사용법
fruit = "사과 참외 수박".split()    # 공백을 기준으로 분리
print(f"분리 전: '사과 참외 수박'")
print(f"분리 후: {fruit}")
print(f"타입: {type(fruit)}")

"""
split() 동작 과정:
1. "사과 참외 수박" 문자열에서 공백을 찾음
2. 공백을 기준으로 문자열을 나눔
3. ['사과', '참외', '수박'] 리스트로 변환
"""

# ========================================
# 다중 입력받기: input().split() 조합
# ========================================

print("\n=== 다중 입력받기 ===")

"""
input().split()을 사용하면:
1. 사용자가 공백으로 구분된 여러 값을 한 번에 입력
2. split()으로 각각 분리
3. 여러 변수에 동시에 할당 가능
"""

print("다음 코드를 실행하면:")
print('fruit1, fruit2, fruit3 = input().split()')
print('print(fruit1, fruit2, fruit3, sep=",")')

# 실제 동작 예시 (주석으로 설명)
# 사용자 입력: "사과 바나나 포도"
# fruit1, fruit2, fruit3 = input().split()
# fruit1 = "사과", fruit2 = "바나나", fruit3 = "포도"
# print(fruit1, fruit2, fruit3, sep=',')  # 출력: 사과,바나나,포도

"""
다중 할당 동작 과정:
1. input()으로 "사과 바나나 포도" 입력받음
2. split()으로 ["사과", "바나나", "포도"]로 분리
3. 각각을 fruit1, fruit2, fruit3에 할당
4. sep=','로 쉼표를 구분자로 사용하여 출력
"""

# ========================================
# 실제 실행 가능한 예제들
# ========================================

print("\n=== 실제 실행 예제 ===")

# 1. 간단한 계산기
print("간단한 계산 예제:")
num1 = 10
num2 = 3
print(f"{num1} + {num2} = {num1 + num2}")
print(f"{num1} - {num2} = {num1 - num2}")
print(f"{num1} * {num2} = {num1 * num2}")
print(f"{num1} / {num2} = {num1 / num2}")

# 2. 복합 할당 연산자 활용
total = 0
print(f"초기값: {total}")

total += 100    # 100 추가
print(f"100 추가: {total}")

total *= 2      # 2배 증가
print(f"2배 증가: {total}")

total -= 50     # 50 차감
print(f"50 차감: {total}")

# 3. 문자열 조작
message = "Python Programming"
words = message.split()  # 공백으로 분리
print(f"원본: {message}")
print(f"분리: {words}")

# 다른 구분자 사용 예시
text1 = "apple,banana,orange"
fruits_comma = text1.split(',')     # 쉼표를 구분자로 사용
print(f"쉼표 분리: {fruits_comma}")

text2 = "2024-12-25"
date_parts = text2.split('-')       # 하이픈을 구분자로 사용
print(f"날짜 분리: {date_parts}")

######################################################################################################
# 실습 3 input 연습하기
'''
사용자로부터 이름과 나이를 입력 받아, 다음 형식으로 출력하기
    안녕하세요. 저는 [이름]이고, [나이]살입니다.
'''

name, age = input("이름과 나이를 입력해주세요. \n :").split()
print(f'안녕하세요. 저는 {name}이고, {age}살입니다.')

######################################################################################################
# 실습 4 입력과 연산 연습하기
'''
1. 사용자로부터 가로과 세로를 입력 받아 넓이와 둘레를 계산하세요.
    넓이 : 200
    둘레 : 60

2. 네 자릿수 정수를 입력 받고, 각 자릿수를 분리하여 출력하세요.
    천의 자리 : 2
    백의 자리 : 1
    십의 자리 : 4
    일의 자리 : 5
'''

# 가로, 세로 길이 입력
width, height = map(int, input("가로와 세로 길이를 입력해주세요 \n : ").split())
# 넓이
Area = width * height
# 둘레
Perimeter = 2 * (width + height)

print(f"""넓이 : {Area}
둘레 : {Perimeter}""")

# 네 자릿수 정수 입력
num = input("네 자릿수 정수를 입력해주세요 \n : ")
num = int(num)
th = num // 1000
hun = (num - (th*1000)) // 100
ten = (num - (th*1000) - (hun*100)) // 10
one = (num - (th*1000) - (hun*100) - (ten*10))
print(f"""천의 자리: {th}
백의 자리: {hun}
십의 자리: {ten}
일의 자리: {one}""")

# ========================================
# 실습 4: 입력과 연산 연습하기 (복합 할당 연산자 활용) / 에단 리더 답변
# ========================================
num = int(input("네 자릿수 정수를 입력해주세요 \n : "))
print(f'천의 자리 : {num // 1000}')
num = num % 1000
print(f'백의 자리 : {num // 100}')
num = num % 100
print(f'십의 자리 : {num // 10}')
num = num % 10
print(f'일의 자리 : {num // 1}')

######################################################################################################
# 실습 5 발표 순서와 발표 주제 정하기
'''
한 조에서 팀 프로젝트 발표 순서와 발표 주제를 정했습니다.
    발표자 이름 3명과 주제 3개를 한 번에 입력 받고, 아래와 같이 출력해보세요.
'''

name1, name2, name3 = input("발표자 이름 3명을 입력해주세요 \n : ").split()
sub1, sub2, sub3 = input("발표 주제 3개를 입력해주세요 \n : ").split()

print('발표 순서 안내입니다. \n', f'1조 발표자: {name1} - 주제: {sub1}',
      f'2조 발표자: {name2} - 주제: {sub2}', f'3조 발표자: {name3} - 주제: {sub3}', sep="\n")

######################################################################################################
# 실습 6 날짜와 시간
'''
년, 월, 일과 시, 분, 초를 한번에 입력 받아서 출력하기
- 첫번째 입력에서 년, 월, 일 입력 (.으로 구분)
- 두번째 입력에서 시, 분, 초 입력 (:으로 구분)

아래와 같이 출력
RE4의 개강일은 2025년 09월 03일
시작 시간은 09시 00분 00초 입니다.
'''

year, month, day = input("년, 월, 일을 입력해주세요. (.으로 구분합니다) \n : ").split(".")
hour, min, sec = input("시, 분, 초를 입력해주세요. (:으로 구분합니다) \n : ").split(":")

if (int(month) < 10):
    month = '0' + month
if (int(day) < 10):
    day = '0' + day
if (int(hour) == 0 or int(hour) < 10 and hour != '00'):
    hour = '0' + hour
if (int(min) == 0 or int(min) < 10 and min != '00'):
    min = '0' + min
if (int(sec) == 0 or int(sec) < 10 and sec != '00'):
    sec = '0' + sec
print(f'RE4의 개강일은 {year}년 {month}월 {day}일',
      f'시작 시간은 {hour}시 {min}분 {sec}초 입니다.', sep='\n')


# ========================================
# 주요 개념 정리
# ========================================

print("\n" + "="*50)
print("주요 개념 정리")
print("="*50)

print("""
1. 복합 할당 연산자:
   - += : 덧셈 후 할당
   - -= : 뺄셈 후 할당  
   - *= : 곱셈 후 할당
   - /= : 나눗셈 후 할당
   - //= : 정수 나눗셈 후 할당
   - **= : 거듭제곱 후 할당
   - %= : 나머지 연산 후 할당

2. 입력 함수:
   - input() : 문자열 입력받기
   - int(input()) : 정수 입력받기
   - float(input()) : 실수 입력받기

3. 문자열 분리:
   - split() : 공백으로 분리
   - split(',') : 쉼표로 분리
   - split('-') : 하이픈으로 분리

4. 다중 할당:
   - a, b, c = input().split()
   - 여러 값을 한 번에 입력받아 각각 할당
""")
